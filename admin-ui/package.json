{
  "name": "supply-admin-ui",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "predev": "python ../tools/project_guard.py frontend",
    "dev": "vite",
    "prebuild": "node scripts/generate-snapshot.js",
    "snapshot:local": "node scripts/generate-snapshot.js --mode=local",
    "snapshot:remote": "node scripts/generate-snapshot.js --mode=remote",
    "snapshot:remote:local": "node scripts/generate-snapshot.js --mode=remote --base-url=http://127.0.0.1:8000",
    "snapshot:strict-remote": "node scripts/generate-snapshot.js --mode=strict-remote",
    "build": "tsc --noEmit && vite build",
    "smoke:ui": "playwright test",
    "test": "vitest run",
    "test:watch": "vitest",
    "preview": "vite preview"
  },
  "dependencies": {
    "react": "^19.2.5",
    "react-dom": "^19.2.5"
  },
  "devDependencies": {
    "@playwright/test": "^1.59.1",
    "@tailwindcss/vite": "^4.2.2",
    "@types/react": "^19.2.14",
    "@types/react-dom": "^19.2.3",
    "@vitejs/plugin-react": "^4.7.0",
    "tailwindcss": "^4.2.2",
    "typescript": "^6.0.2",
    "vite": "^6.4.1",
    "vitest": "^4.1.7"
  }
}
