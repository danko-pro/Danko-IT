import type { ScreenKey } from "../shared/types";

export const editorNavigationItem: { key: ScreenKey; label: string; note: string } = {
  key: "editor",
  label: "Редактор",
  note: "Пустое рабочее поле",
};

